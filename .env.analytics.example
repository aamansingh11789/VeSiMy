# PostHog — product analytics
# Get your key from posthog.com (free plan, no card needed)
# Create account → New Project → Copy Project API Key
NEXT_PUBLIC_POSTHOG_KEY=phc_REPLACE_WITH_YOUR_KEY
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Vercel Analytics is auto-enabled when deployed to Vercel — no env var needed
